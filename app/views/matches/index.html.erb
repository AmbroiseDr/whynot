<div class="container">
  <div class="top-bar">
    <div class = "logo"><%= cl_image_tag("llpfrsdiwc27yqhw6j0v", width: 45, height: 40) %></div>
    <%=  link_to destroy_user_session_path, method: :delete do%>
    <i class="fas fa-cog"></i>
    <%end %>
  </div>
  <% if current_user.questions_not_answered.size != 0 %>
    <%= link_to question_path(current_user.questions_not_answered.sample) do %>
      <div class="reload-btn">
        <i class="fas fa-sync-alt"></i>
      </div>
    <%end%>
  <% else %>
    <div class="reload-btn">
      <i class="fas fa-sync-alt"></i>
    </div>
  <% end %>
  <div class="cards-matches">
    <% @matches.first(4).each do |match| %>
      <%= link_to match_path(match) do %>
       <%= render partial: "shared/card", locals:{match: match} %>
      <% end %>
    <%end %>
  </div>
</div>
